The Trigger folder holds subfolders for the different reconstruction methods that have been built for P-ONE

Folders:
	linefit = The simple line fit method used as a seed for higher level track reconstructions. 
	llh = Likelihood fitter for track reconstruction
	nutau = the cascade reconstruction.
